syntax = "proto3";

package sim.proto.route;

message Route {
    string name = 1;
    string window_name = 2;

    message Point {
        double latitude = 1;
        double longitude = 2;
    }

    repeated Point points = 3;
}
